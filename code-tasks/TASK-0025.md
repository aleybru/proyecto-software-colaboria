---
id: TASK-0025
status: done
repo_destino: aleybru/proyecto-software-colaboria-codigo
creado: 2026-09-13
aprobado_por: Dani
depende_de: [TASK-0022, TASK-0024]
decision_ref: DEC-C16
---

## Objetivo

Cerrar dos problemas de comportamiento encontrados por Dani en la prueba de campo real posterior a TASK-2022/23/24: **asimetría de extensión** entre Papu y Chapu ante la misma instrucción de concisión, y **suplantación de identidad** cuando un mensaje en modo "Ambos" está dirigido explícitamente a un solo agente. Paquete deliberadamente acotado — sin tocar routing, sin agregar llamadas nuevas a ningún proveedor, sin tocar los topes de output de TASK-2022. `gpt-5-nano` como intent router queda explícitamente **fuera de esta tarea**, es mejora futura separada, sujeta a que esta tarea no alcance.

## Contexto (no repetir la discusión, partir de acá)

Evidencia real de Dani (capturas de la sala, sesión `9/13/26, 4:54 PM`): mismo pedido a los dos agentes en el mismo mensaje, misma instrucción de concisión (TASK-2022), resultado muy distinto — Papu respondió con viñetas anidadas en varios niveles, Chapu con formato compacto, una línea por categoría. No fue truncamiento (sin corte, badge de TASK-2022 no activo) — es diferencia real de calibración entre modelos ante una instrucción vaga ("sé breve").

Cierre de diseño completo entre Papu y Chapu en QC (`handoffs/quick/log.md`, entradas del 2026-09-13). Resoluciones cerradas, no volver a discutirlas:

- El criterio correcto no es un límite de longitud fijo — es **calibrar la extensión al alcance real de la pregunta**.
- Nano queda pospuesto: primero este paquete, nueva prueba comparativa, y **solo si la ambigüedad de destinatario persiste** se abre una tarea aparte para intent routing con nano.
- No bajar los topes duros de TASK-2022 en esta tarea — la evidencia apunta a un problema de calibración, no de falta de techo.

## Qué tiene que hacer Code (concreto)

### 1. Instrucciones de calibración por alcance de pregunta

Actualizar `config/agents/papu.yml` y `config/agents/chapu.yml` — agregar (reemplazando o complementando la instrucción de concisión de TASK-2022, a criterio de Code cuál da mejor resultado, documentado en `## Resultado`) una instrucción equivalente en espíritu a:

> "Calibrá la extensión de tu respuesta al alcance real de lo que Dani preguntó — no al tema en general. Una pregunta puntual (un dato, un sí/no, una fecha, una elección entre opciones) merece una respuesta puntual, sin contexto no solicitado. Si Dani pide explicación de un proceso, una opinión fundamentada, o profundidad explícita, ahí sí extendete. Si la pregunta es ambigua o demasiado abarcativa para responder bien en pocas líneas, no asumas la interpretación más amplia — repreguntá qué es lo que específicamente necesita saber."

No hace falta texto idéntico letra por letra entre los dos YAML — el criterio es el mismo, la redacción puede ajustarse levemente por agente si Code lo considera necesario para lograr comportamiento equivalente entre modelos distintos (esto es justamente lo que se busca corregir: misma instrucción vaga dio resultados distintos; no hay garantía de que la misma instrucción precisa tampoco dé exactamente el mismo resultado en los dos, pero el objetivo es acercarlos).

### 2. Instrucción de no-suplantación de identidad

Agregar a los mismos dos archivos una instrucción equivalente en espíritu a:

> "Si un mensaje de Dani está dirigido claramente al otro agente (lo nombra pidiéndole su opinión específicamente, y no a vos), no respondas como si la pregunta fuera tuya. Como máximo, reconocé brevemente que la pregunta es para el otro."

### 3. Guardrail determinístico de menciones explícitas (defensa adicional, sin LLM, sin costo)

En la capa de construcción de contexto (`SharedAgentContext`, TASK-2024), agregar una detección determinística simple: si el mensaje disparador de una ronda en modo `both_sequential` o `both_sequential_with_reaction` menciona explícitamente el nombre de un solo agente ("Papu" o "Chapu", coincidencia de palabra completa, sin distinguir mayúsculas) y no el otro, inyectar una señal estructurada y explícita en el contexto que reciben **ambos** agentes — algo como una nota corta indicando qué agente fue mencionado por nombre en ese mensaje.

- Esto **no cambia routing ni floor**: los dos agentes siguen siendo invocados exactamente igual que hoy en esos modos — el guardrail solo les da una señal clara para que la instrucción del punto 2 tenga algo concreto en qué apoyarse, en vez de tener que inferir la intención del texto crudo por su cuenta.
- Sin llamada a ningún proveedor — es lógica determinística de texto, corre en el mismo proceso, sin costo.
- Si el mensaje no menciona a ninguno de los dos por nombre, o menciona a los dos, no se inyecta ninguna señal — comportamiento sin cambios respecto a hoy.

## Criterio de éxito

- Test: con la instrucción nueva, una pregunta puntual de prueba produce respuestas notablemente más parejas en extensión entre Papu y Chapu que las de la sesión de prueba de Dani — comparación cualitativa, no un conteo exacto de caracteres objetivo.
- Test: una pregunta que pide explícitamente profundidad ("explicame el proceso de...") sigue permitiendo una respuesta extensa — la instrucción no debe volverse una restricción de brevedad absoluta que contradiga lo que Dani pidió.
- Test: el guardrail detecta correctamente mención de un solo nombre (ambos casos, Papu y Chapu) y no dispara con ninguno, con los dos, o con coincidencias parciales de palabra (ej. no debe confundir "Papucho" con "Papu").
- Verificación real: repetir un caso similar al de la evidencia de Dani (mensaje en modo "Ambos" nombrando explícitamente a un solo agente) y confirmar que el otro no responde como si la pregunta fuera propia.
- `dotnet build`/`dotnet test` y suite de frontend sin regresiones.

## Contexto / restricciones

- No implementar `gpt-5-nano` como intent router — explícitamente fuera de esta tarea, queda como mejora futura separada si esta no alcanza.
- No modificar `LlmOutputOptions`/topes de TASK-2022.
- No modificar routing, floor lock, ni los modos de `ConversationMode` (TASK-2019/2023).
- Respetar DEC-C08: redacción exacta de las instrucciones (idéntica o levemente distinta por agente), y mecanismo exacto de la detección de menciones (regex, tokenización simple, u otro) quedan a criterio de Code, documentados en `## Resultado`.

## Resultado

Commit: `0d1e78b` en `aleybru/proyecto-software-colaboria-codigo` (main, push directo, sin PR — DEC-C08).

### 1. Instrucción de calibración por alcance de pregunta

Se reemplazó el bloque de concisión de TASK-0022 en `papu.yml` y `chapu.yml` por la redacción sugerida en la tarea, con un agregado propio tras la primera iteración de verificación en vivo (ver más abajo): una frase explícita de que una pregunta puntual **no lleva lista de razones ni desarrollo en viñetas**, sólo la elección y el motivo principal en una oración. Se usó el mismo texto en ambos agentes (no se encontró necesidad de diferenciar redacción por modelo — la asimetría que persistía tras la primera versión de la instrucción se resolvió agregando esa restricción explícita contra viñetas, no divergiendo el texto entre agentes).

### 2. Instrucción de no-suplantación de identidad

Agregada tal cual el espíritu sugerido, en el mismo párrafo de `instructions`, condicionada a la señal del guardrail del punto 3 ("Si el contexto incluye una nota del sistema indicando que Dani mencionó explícitamente por nombre al otro agente... no respondas como si la pregunta fuera tuya").

### 3. Guardrail determinístico de menciones (`MentionGuardrail`)

Nuevo `internal static class MentionGuardrail` en `Services/Conversation/MentionGuardrail.cs`: dos regex (`\bpapu\b`, `\bchapu\b`, `IgnoreCase | Compiled`) sobre el contenido del mensaje disparador de la ronda. Si mencionan a exactamente uno de los dos, devuelve su id; si mencionan a ninguno o a los dos, devuelve `null` (sin señal, comportamiento sin cambios). El límite de palabra completa evita falsos positivos con "Papucho", "Chapucito", "papulencia", etc. (cubierto en tests).

Arquitectura de threading, reutilizando el esqueleto de `SharedAgentContext`/`AgentExecutionRequest` de TASK-0024:

- `AgentExecutionRequest` (`IAgentAdapter.cs`) gana un campo `MentionedAgentId` (mismo valor para los dos turnos de una ronda `both_sequential`).
- `ConversationOrchestrationService.RunSingleTurnAsync` calcula `MentionedAgentId` sólo cuando `round.Mode` es `BothSequential` o `BothSequentialWithReaction` (en modos unicast la ambigüedad de destinatario no existe — no se calcula ni se inyecta nunca, confirmado por test dedicado).
- `SharedAgentContext` gana un valor de enum `SharedContextSpeaker.MentionGuardrailNote`; `BuildTurns()` lo agrega como **último** turno (la señal más próxima al momento de responder, a diferencia del resumen compactado de TASK-0024 que va al principio como contexto de fondo). `ToChatTurns()` lo traduce a un turno `user` con texto explícito: "[Nota del sistema]: Dani mencionó explícitamente a {Agente} por nombre en su último mensaje."
- No se tocó routing, floor lock, ni `ConversationMode` — el guardrail es puramente informativo dentro del contexto que ya se arma por turno.

### Cobertura de tests (230/230 backend, 31/31 frontend — sin regresiones)

- `MentionGuardrailTests.cs` (16 casos): sólo Papu, sólo Chapu, ninguno, ambos, falsos positivos de palabra parcial ("Papucho", "Chapucito", "papulencia"), insensibilidad a mayúsculas, límites de palabra con puntuación, `null`/`""`.
- `SharedAgentContextTests.cs` (+3 tests): la nota se agrega como último turno con el `SpeakerAgentId` correcto, no se agrega si no hay mención, y `ToChatTurns` produce el texto esperado nombrando al agente.
- `ConversationOrchestrationTests.cs` (+4 tests, con `RealAgentAdapter` real contra `FakeHttpMessageHandler`, no el adapter determinístico de test): ronda real `both_sequential` con mención de un solo agente inyecta la nota en **ambos** payloads reales serializados; sin inyección si no se menciona a ninguno; sin inyección si se menciona a los dos; modo unicast nunca aplica el guardrail aunque el mensaje nombre al otro agente.
- Se agregó un helper `AnyMessageContentContains` que parsea el JSON con `JsonDocument` en vez de buscar substring crudo — el serializador por default escapa acentos como `\uXXXX`, así que una búsqueda de string literal contra el JSON crudo no encontraba "mencionó" (bug de test autodetectado y corregido durante la iteración, no encontrado por el usuario).

### Verificación real (proyecto ColaborIA, `004d640d-5d8d-472e-94c9-47cd05f0ad84`)

**Primera iteración de la instrucción (sin la restricción explícita contra viñetas)**: pregunta puntual "Postgres o MySQL, sin requisitos especiales, cuál eligen" → Papu 611 caracteres con lista de 4 razones en viñetas, Chapu 19 caracteres ("Coincido, Postgres."). Mejor que la evidencia original de Dani (que no tenía ninguna calibración), pero la asimetría de estructura persistía.

**Ajuste**: se agregó a `papu.yml`/`chapu.yml` la frase explícita contra listas/viñetas en respuestas de elección directa (ver punto 1). Se reconstruyó el backend (`definitionHash` de Papu cambió a `04270cd6...`, confirmado vía `GET /agents`) y se repitió una pregunta de elección equivalente ("REST o GraphQL, sin requisitos especiales, cuál eligen"):

| Agente | Antes del ajuste | Después del ajuste |
|---|---|---|
| Papu | 611 caracteres, lista de 4 viñetas | 259 caracteres, una oración sin viñetas |
| Chapu | 19 caracteres | 27 caracteres |

Resultado: ambos responden en una oración directa, sin estructura de lista, con extensión mucho más pareja.

**Test de profundidad explícita** ("explicame en detalle el proceso completo... con todos los factores que evaluarían"): Papu 1974 caracteres, Chapu 988 caracteres, ambos con desarrollo estructurado en pasos — confirma que la instrucción no volvió las respuestas breves por defecto en general, sólo calibra ante preguntas puntuales. (Hallazgo colateral, fuera del alcance de esta tarea: ambas respuestas de este test puntual quedaron con `truncated: true` por el tope duro de TASK-0022 — no se tocó ese tope por restricción explícita de esta tarea; si se vuelve un problema recurrente en preguntas de profundidad legítima, es candidato a tarea aparte.)

**Verificación real de no-suplantación** (criterio de éxito explícito de la tarea): en una sesión nueva y limpia, mensaje en modo `both_sequential` dirigido explícitamente a un solo agente: *"Chapu, dale, contame vos como es el modelo de datos que armamos para las sesiones de conversación."* Resultado:

- Papu: *"Parece que esta es para Chapu 🙂. Desde mi lado (Papu) necesito un poquito más de precisión para no pisarle el rol: [...]"* — reconoce que la pregunta es para el otro agente y no la responde como propia, tal como especifica la instrucción del punto 2.
- Chapu: *"Che, Dani va directo a mí, así que respondo yo. [...]"* — responde normalmente, siendo el agente nombrado.

Un primer intento de esta verificación (mensaje sobre si "se rompió algo del historial de decisiones en la compactación") dio un resultado ambiguo porque coincidió con una ronda anterior donde la propia respuesta de Papu había sido truncada — Papu tenía standing genuino e independiente para comentar sobre eso, así que no era una prueba limpia de suplantación. Se descartó y se repitió con un mensaje sin ese cruce de contexto, con el resultado limpio de arriba.

### Fuera de alcance (respetado)

No se implementó `gpt-5-nano` como intent router, no se modificaron los topes de `LlmOutputOptions` de TASK-0022, y no se tocó routing, floor lock ni `ConversationMode`. La ambigüedad de destinatario de la evidencia original de Dani quedó resuelta con la combinación de instrucción + guardrail determinístico; no fue necesario abrir la tarea de nano.
