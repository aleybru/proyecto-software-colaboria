---
id: TASK-0018
status: done
repo_destino: aleybru/proyecto-software-colaboria-codigo
creado: 2026-08-30
aprobado_por: Dani
depende_de: [TASK-0007, TASK-0016]
decision_ref: DEC-C10, DEC-C16
---

## Objetivo

Implementar el núcleo persistente del canal conversacional (Bloque D de DEC-C16, primer corte): modelo de datos y contrato mínimo para `sessions`, `messages` y `agent_turns`, con aislamiento estricto por proyecto e idempotencia. **Sin routing, sin `both_sequential`, sin adapters de proveedor, sin llamadas reales a OpenAI/Anthropic, sin frontend de sala, sin Context Builder real, sin approvals/gateway.**

## Contexto de diseño (resultado del contraste Papu/Chapu, no repetir la discusión — partir de acá)

Contraste completo en `handoffs/quick/log.md`, entradas del 2026-08-30. Resoluciones ya cerradas:

- `input_context_ref` queda **nullable** en esta tarea — DEC-C10 punto 6 exige que sea "trazable y verificable, no texto decorativo"; sin Context Builder real, forzarlo `NOT NULL` obligaría a guardar un placeholder. Se vuelve obligatorio cuando exista Context Builder (tarea futura), no acá.
- Los agentes (`papu`/`chapu`) **no son una tabla de Postgres** — son configuración en memoria (`AgentRegistry`, TASK-0016). La validación de `agent_id` en `messages`/`agent_turns` es de **aplicación**, no una FK real de Postgres.
- **Conversar no pasa por `AgentAuthorizationService`** (TASK-0017). Esa política regula el techo de efectos sobre `project_state`/Drive/repo de código — conversación no es estado ni efecto secundario por sí misma (protocolo común, sección 8: "conversación no es estado").
- Completar un turno exitoso debe ser **una única transacción de DB**, no una secuencia de escrituras separadas.

## Qué tiene que hacer Code (concreto)

### 1. Modelo de datos (EF Core + Postgres)

**`sessions`:**
- `id`
- `project_id` (obligatorio — DEC-C10 punto 1; FK real a `projects`, TASK-0007)
- `created_at`, `updated_at`

**`messages`** (fuente de verdad del contenido — DEC-C10 punto 2):
- `id`
- `session_id` (FK real a `sessions`)
- `role`: `user` | `agent` (V0 — sin otros roles todavía)
- `agent_id`: string, **requerido solo cuando `role = agent`**, validado en aplicación contra `AgentRegistry` (sin FK real — ver contexto de diseño)
- `content`
- `client_message_id`: requerido cuando `role = user`, **único por sesión** (idempotencia — DEC-C10 punto 3)
- `created_at`

**`agent_turns`** (bitácora de cómo se generó una respuesta — DEC-C10 punto 2):
- `id`
- `session_id` (FK real)
- `agent_id` (string, validado en aplicación)
- `agent_definition_hash` (de TASK-0016 — ancla el turno a la versión exacta del perfil usado)
- `provider`, `model` (snapshot del `RuntimeBinding` usado, no una referencia mutable)
- `status`: `running` | `succeeded` | `failed` | `timeout`
- `started_at`, `finished_at` (nullable hasta terminar)
- `timeout_at` (explícito — DEC-C10 punto 4, ningún turno queda indefinidamente en `running`)
- `input_context_ref` (**nullable** — ver contexto de diseño)
- `error` (nullable)
- `output_message_id` (FK nullable a `messages` — solo se completa si `status = succeeded`)

### 2. Atomicidad (requisito explícito, no implícito)

Completar un turno exitoso (`agent_turn.status = succeeded` + creación del `message` del agente + `agent_turn.output_message_id` apuntando a ese mensaje) tiene que ejecutarse dentro de **una única transacción de base de datos**. Un fallo a mitad de camino no puede dejar un `agent_turn` en `succeeded` sin `message`, ni un `message` de agente sin `agent_turn` que lo respalde.

### 3. API mínima (para verificar el slice, no como superficie pública final)

- Crear sesión dentro de un proyecto.
- Persistir un mensaje de Dani, idempotente por `client_message_id` dentro de la sesión (reintentar con el mismo `client_message_id` no duplica).
- Leer transcript de una sesión (mensajes en orden).

`agent_turns` queda como servicio interno — **sin endpoint HTTP público todavía** en esta tarea (no hay adapters que lo disparen desde afuera).

### 4. Aislamiento estricto por proyecto

Ninguna consulta de sesión, mensaje o turno puede cruzar el límite de proyecto — el proyecto B no puede leer sesiones, mensajes ni transcript del proyecto A, ni por error de query ni por parámetro manipulado.

## Criterio de éxito

- Migración de EF Core real aplicada contra Postgres, con las FKs/constraints descriptas (incluida la unicidad de `client_message_id` por sesión).
- Reiniciar el backend conserva sesiones/mensajes/turnos ya persistidos (no es estado en memoria).
- Reintentar el mismo `client_message_id` dos veces en la misma sesión no duplica el mensaje.
- Un proyecto B, mediante cualquier combinación de IDs, no puede leer sesión, mensajes ni transcript de un proyecto A — verificado con un test que lo intente explícitamente.
- Test de atomicidad: forzar una excepción entre la creación del `message` del agente y la actualización de `agent_turn.output_message_id` (o el paso equivalente), y confirmar que **no** queda ni un `message` huérfano ni un `agent_turn.status = succeeded` sin `message` vinculado.
- Un `agent_turn` en `running` que supera su `timeout_at` no queda indefinidamente así — pasa a `timeout` de forma verificable (mecanismo exacto a criterio de Code, documentado en `## Resultado`).
- `dotnet build` sin errores; `dotnet test` sin regresiones sobre la suite existente (TASK-0001 a TASK-0017).

## Contexto / restricciones

- No implementar floor lock, `both_sequential`, adapters de proveedor, llamadas reales a OpenAI/Anthropic, frontend de sala, Context Builder real, approvals ni gateway — todo eso es alcance de tareas posteriores del Bloque D/E.
- No crear un endpoint HTTP público para disparar `agent_turns` — no hay nada todavía que lo consuma de forma real.
- No forzar `input_context_ref` a `NOT NULL` ni completarlo con un valor decorativo — debe quedar nullable, sin excepciones, hasta que exista Context Builder real.
- No intentar una FK de Postgres hacia una tabla de agentes — no existe tal tabla; `agent_id` se valida en aplicación contra `AgentRegistry` (TASK-0016).
- Respetar DEC-C08: detalles técnicos menores (mecanismo exacto de detección de timeout, forma interna de la transacción) quedan a criterio de Code, documentados en `## Resultado`. Si aparece una contradicción real con DEC-C10/DEC-C16 durante la implementación, señalarla explícitamente en vez de resolverla por criterio propio.

## Resultado

**Status: done.**

Ejecutado y commiteado directamente a `main` del repo de código (sin PR — commit directo): `4f430f5` — "TASK-0018: nucleo conversacional persistente (Bloque D, primer corte)". Ninguna contradicción nueva encontrada contra DEC-C10/DEC-C16 durante la implementación.

### Modelo de datos y migración

`sessions`, `messages`, `agent_turns` tal cual especifica la tarjeta (`Models/Session.cs`, `Message.cs`, `MessageRole.cs`, `AgentTurn.cs`, `AgentTurnStatus.cs`; mapeo en `Data/ColaborIaDbContext.cs`). Migración real `AddConversationCore` generada y **aplicada contra Postgres real** (`dotnet ef database update`).

Reforzado a nivel de **constraint de DB**, no sólo validación de aplicación:
- `ck_messages_role_fields`: `role=user` exige `client_message_id` y prohíbe `agent_id`; `role=agent` exige `agent_id` y prohíbe `client_message_id`.
- Índice único parcial `ix_messages_session_id_client_message_id` sobre `(session_id, client_message_id) WHERE client_message_id IS NOT NULL` — unicidad **por sesión**, no global.
- `ck_agent_turns_succeeded_has_output`: `status=succeeded` exige `output_message_id`; cualquier otro estado lo prohíbe.

`agent_id` en `messages`/`agent_turns` **no es FK real** — se valida en aplicación contra `AgentRegistry` (TASK-0016), tal como pedía la tarjeta. `input_context_ref` quedó **nullable**, sin placeholder.

### Arquitectura: dos servicios

- **`ConversationService`** (`Services/Conversation/ConversationService.cs`): `CreateSessionAsync`/`PostUserMessageAsync` (idempotente)/`GetTranscriptAsync`. Las tres operaciones exigen que la sesión pertenezca al `projectId` recibido — aislamiento estricto verificado con tests que intentan cruzar el límite explícitamente.
- **`AgentTurnService`** (`Services/Conversation/AgentTurnService.cs`, servicio interno, **sin endpoint HTTP**): `StartTurnAsync`, `CompleteTurnAsync` (atómico — ver abajo), `FailTurnAsync`, `ExpireOverdueTurnsAsync`.

`SessionsEndpoints.cs` es un adaptador HTTP delgado sobre `ConversationService` — la lógica de negocio no vive en el archivo de endpoints (mismo criterio que el resto del backend).

**Conversar no pasa por `AgentAuthorizationService`** (TASK-0017), tal como se cerró en el contraste Papu/Chapu del 2026-08-30 — esa política regula el techo de efectos sobre `project_state`/Drive/repo de código, no la conversación en sí.

### Atomicidad (criterio de éxito explícito)

`AgentTurnService.CompleteTurnAsync` envuelve la creación del `message` del agente + `status=succeeded` + `output_message_id` en una única transacción de DB real (`BeginTransactionAsync`/`CommitAsync`/`RollbackAsync`). Se agregó un parámetro `simulateFailureAfterMessageCreated` (delegado opcional, `null` por default — **nunca se pasa en código de producción**, sólo lo usan los tests) para forzar una excepción real exactamente entre la creación del `message` y la actualización de `agent_turn`. Verificado con una conexión **nueva** (no el mismo `DbContext`) que tras ese fallo forzado: el turno sigue `running`, sin `output_message_id`, sin `finished_at`, y el `message` con contenido distintivo no quedó persistido en ningún lado.

### Timeout (mecanismo a criterio de Code, DEC-C08)

`ExpireOverdueTurnsAsync` es un **sweep explícito y verificable**, invocable bajo demanda (no un timer/`IHostedService` en background). Razón: en este primer corte no existe todavía ningún adapter real que deje turnos `running` de verdad — no hay nada que un scheduler en background necesite vigilar todavía. Un scheduler que llame a este método periódicamente queda para cuando exista ejecución real de proveedor (tarea futura). Verificado con un turno con `timeout_at` ya vencido (transiciona a `timeout`) junto a otro con `timeout_at` futuro (no lo toca).

### Tests

**Primera vez que la suite corre contra Postgres real**, no sólo unit tests puros — necesario porque "sobrevive a un restart" y "rollback transaccional real" no se pueden probar de otra forma sin falsificar la prueba (un provider en memoria no tiene transacciones reales ni pierde estado al "reiniciar"). `TestDb.cs` lee la connection string vía `dotnet user-secrets`, compartiendo el mismo `UserSecretsId` de `ColaborIaDbContext` — nunca hardcodeada en el archivo de test.

`dotnet build`: 0 errores. `dotnet test`: **124/124** (114 preexistentes de TASK-0016/0017 sin regresiones + 10 nuevos), incluyendo explícitamente todo lo pedido en `## Criterio de éxito`:

- sesión/mensaje/turno releídos con una conexión **nueva** (no el mismo `DbContext`) — confirma que no es sólo estado en memoria;
- reintentar el mismo `client_message_id` no duplica (a nivel de aplicación) — y el índice parcial de Postgres lo rechaza igual si se intenta insertar directo, evitando el chequeo de aplicación (defensa en profundidad);
- constraint de `role`/campos rechazado directamente a nivel de DB;
- proyecto B no puede leer ni escribir en la sesión de proyecto A, verificado con un `sessionId` real de A pasado con el `projectId` de B;
- turno completado exitosamente: `message` + `succeeded` + `output_message_id`, releído con conexión nueva;
- fallo forzado entre crear el `message` y actualizar `agent_turn`: rollback real confirmado (ver arriba);
- sweep de timeout transiciona sólo los turnos vencidos;
- `input_context_ref` nullable sin placeholder.

**Verificado además en vivo, con un restart real del proceso** (no sólo un `DbContext` nuevo): se creó un proyecto/sesión/mensaje, se mató el proceso del backend de verificación, se levantó uno completamente nuevo, y el transcript se leyó igual. Aislamiento entre dos proyectos reales confirmado vía HTTP (`404` en ambos sentidos: lectura y escritura cruzada). Logs revisados: sin secrets ni excepciones.

### Limitaciones reales / hallazgo colateral

- Ninguna limitación nueva respecto de lo ya acotado por la tarjeta (sin routing, adapters, LLMs reales, frontend, Context Builder real, approvals/gateway — todo explícitamente fuera de alcance).
- **Hallazgo no relacionado con esta tarea, detectado durante la verificación:** al confirmar que no quedaban proyectos de prueba (`GET /projects`), se encontró que los proyectos reales "Atlas Showcase" y "Tigre de papel" —presentes y confirmados como reales hasta el cierre de TASK-0014— ya no existen en la base real; sólo queda "ColaborIA". Nada en TASK-0016, TASK-0017 ni TASK-0018 los tocó (ningún `DELETE` se ejecutó contra esos IDs). Coincide en el tiempo con el corte de energía reportado entre sesiones — que en su momento sólo se verificó a nivel de integridad de `git`, nunca a nivel de integridad de datos de Postgres. Reportado a Dani directamente en el chat; no se investigó más a fondo por estar fuera del alcance de esta tarjeta.
