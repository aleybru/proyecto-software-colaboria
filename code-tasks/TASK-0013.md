---
id: TASK-0013
status: done
repo_destino: aleybru/proyecto-software-colaboria-codigo
creado: 2026-08-29
aprobado_por: Dani
depende_de: [TASK-0012]
decision_ref: DEC-C09, DEC-C10, DEC-C11, DEC-C16
reconstruccion_retrospectiva: true
id_historico_en_commits: "TASK-0012"
---

## Objetivo

**Nota de trazabilidad:** reconstrucción retrospectiva, escrita el 2026-08-30. Identificada en los commits del repo de código como **"TASK-0012"** — se conserva ese ID histórico como alias; el ID canónico en `code-tasks/` es **TASK-0013**.

Alinear la gestión de proyectos a la topología fijada por la revisión del 2026-08-29 de DEC-C09/DEC-C10/DEC-C11/DEC-C16: el alta de proyecto deja de ser un editor genérico de recursos. `governance_repo` queda deprecado para altas nuevas; `narrative` solo crea la carpeta raíz de Drive; `software` crea Drive + un único `code_repo` (nunca gobernanza, nunca un segundo repo).

## Qué se hizo (reconstruido de la evidencia real)

**Backend:**
- `ResourceCatalog`: `narrative = [drive_root]`, `software = [drive_root, code_repo]`. `governance_repo` deja de ser `kind` válido para altas/vínculos nuevos.
- `POST /projects` aprovisiona la topología completa en la misma request (crea las filas y llama a `ProvisionOneAsync` para cada una, secuencial). Un fallo parcial no invalida el proyecto — cada recurso queda con su estado real.
- Convención de nombres actualizada (DEC-C09 secciones 4 y 5 revisadas): el repo se llama como el proyecto slugificado, sin sufijo `-codigo`; la carpeta de Drive, como el proyecto tal cual, sin prefijo `"Proyecto "`.
- Recursos legacy (ej. `governance_repo` de proyectos creados antes de este cambio) se muestran igual en el detalle, pero no cuentan para el estado general (`ComputeOverallStatus` se limita a los `kinds` de la topología vigente).

**Frontend:**
- `project-create`: vuelve a ser solo Nombre + Tipo + "Crear proyecto" — se elimina el formulario de slots con crear/vincular, tipo de recurso y proveedor que había agregado TASK-0011 (histórica "TASK-0010").
- `project-detail`: se elimina el bloque de vincular/cambiar vínculo/desvincular del flujo normal — un recurso activo solo muestra "Abrir"; uno pendiente/con error muestra "Reintentar" (o "Reconectar Google Drive" para `reauthorization_required`).
- La capacidad de vincular/desvincular sigue existiendo en el backend para migración/dogfooding, pero ninguna pantalla normal la expone.

## Resultado

**Status: done.**

Ejecutado y commiteado directamente a `main` (sin PR — commit directo).

**Evidencia verificable (citada del mensaje de commit original, con pruebas en vivo documentadas contra sistemas reales, no simuladas):**
- Commit: `5b757e1` — "TASK-0012: alinea gestion de proyectos a la topologia vigente (DEC-C09/C10/C11/C16)" (2026-08-29T20:52:21Z, autor `aleybru`).
- Proyecto `narrative` real creado desde la UI real → exactamente 1 recurso (Drive, activo, URL real), cero llamadas a GitHub.
- Proyecto `software` real creado desde la UI real → exactamente 2 recursos (Drive activo con URL real; `code_repo` en `provisioning_error` por la limitación de GitHub Apps en cuentas personales, resuelta después en TASK-0014). Nunca se creó `governance_repo`.
- El proyecto legacy real "ColaborIA" (creado antes de este cambio) siguió devolviendo sus 3 recursos reales, incluido `governance_repo`, con `overallStatus: completo` — el legacy no lo arruina.
- `dotnet build` + `dotnet test`: 63/63 OK. `ng build` + `ng test`: 7/7 OK.
- Sin tokens en el log del proceso durante toda la sesión de pruebas.

**Reconstrucción retrospectiva** — contenido tomado íntegramente del mensaje de commit original (`5b757e1`), que incluye su propia sección de verificación en vivo. No se agregó ningún dato que no esté en ese mensaje.
