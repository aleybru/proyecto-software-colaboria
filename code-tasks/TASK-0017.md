---
id: TASK-0017
status: done
repo_destino: aleybru/proyecto-software-colaboria-codigo
creado: 2026-08-30
aprobado_por: Dani
depende_de: [TASK-0016]
decision_ref: DEC-C04, DEC-C11, DEC-C16
---

## Objetivo

Implementar la segunda capa del Bloque C de DEC-C16: una política **estática y determinística** de autorización de agentes — el techo de autoridad por `agente + tipo de recurso + operación`. Sin evidencia de aprobación dinámica, sin gateway, sin efectos reales.

**Boundary de capas (no implementar más de lo que corresponde a esta tarea):**

```
IDENTIDAD   → AgentRegistry (TASK-0016)
POLÍTICA    → esta tarea (TASK-0017)
APROBACIÓN  → evidencia dinámica (tarea futura)
GATEWAY     → enforcement y efectos reales (tarea futura)
```

## Qué tiene que hacer Code (concreto)

### 1. Contrato de política

Definir `PolicyEvaluationResult` con:

- `status`: `evaluated` | `invalid_request` | `unavailable`.
- `decision` (presente **solo** cuando `status = evaluated`): `allowed_without_approval` | `requires_approval` | `denied`.
- `reason_code`: estable, machine-readable.
- `detail`: texto humano corto.
- `policy_refs`: **lista** de Decisions que sustentan el resultado (no un único campo).

No mezclar en un solo enum la decisión de negocio con el error de evaluación — `status` y `decision` son campos separados, tal como arriba.

### 2. Vocabulario propio de autorización (no reutilizar el de recursos)

**No reutilizar** `ProjectResource.Kind` ni `ResourceCatalog` (de TASK-0011/0013 históricas). La política tiene su propio vocabulario, deliberadamente desacoplado de la existencia física de un recurso:

- `resource_kind`: `project_state` | `project_drive` | `project_code_repo`.
- `operation` (V0): `read` | `write`.

La policy define **techo de autoridad**, no existencia física. Ejemplo: `chapu + project_code_repo + read` puede resolver `allowed_without_approval` aunque el proyecto sea `narrative` y no tenga repo — la existencia real del recurso es responsabilidad del gateway (tarea futura), no de esta política.

### 3. Matriz obligatoria (12 celdas)

**Papu:**

| resource_kind | operation | decision |
|---|---|---|
| `project_state` | `read` | `allowed_without_approval` |
| `project_state` | `write` | `requires_approval` |
| `project_drive` | `read` | `allowed_without_approval` |
| `project_drive` | `write` | `denied` |
| `project_code_repo` | `read` | `allowed_without_approval` |
| `project_code_repo` | `write` | `denied` |

**Chapu:**

| resource_kind | operation | decision |
|---|---|---|
| `project_state` | `read` | `allowed_without_approval` |
| `project_state` | `write` | `requires_approval` |
| `project_drive` | `read` | `allowed_without_approval` |
| `project_drive` | `write` | `requires_approval` |
| `project_code_repo` | `read` | `allowed_without_approval` |
| `project_code_repo` | `write` | `requires_approval` |

**Regla central:** una aprobación de Dani puede satisfacer `requires_approval`. Una aprobación puntual **nunca** transforma `denied` en permitido — para superar un `denied` hace falta cambiar la política gobernada mediante nueva Decision/tarea, no una aprobación circunstancial.

### 4. Arquitectura interna: dos capas separadas

**A. `PurePolicyMatrix`** — pura y determinística. **Sin** DB, sin red, sin DI, sin `AgentRegistry`, sin topología de proyecto, sin prompts/contexto, sin `provider`/`model`, sin `skills`/`instructions`. Recibe únicamente `agent_id + resource_kind + operation` **ya válidos** y devuelve la celda correspondiente. Esta capa **no conoce** el estado `deprecated` de un agente — eso se resuelve en la capa B.

**B. `AgentAuthorizationService`** — envoltorio que:
1. verifica el estado del `AgentRegistry` (TASK-0016);
2. resuelve el agente;
3. valida el estado del agente;
4. valida `resource_kind`/`operation`;
5. si todo es válido, delega en `PurePolicyMatrix`.

Casos de la capa B:

| Caso | `status` | `decision` | `reason_code` |
|---|---|---|---|
| `AgentRegistry` unavailable | `unavailable` | — | `agent_registry_unavailable` |
| `agent_id` desconocido | `invalid_request` | — | `unknown_agent` |
| `resource_kind` desconocido | `invalid_request` | — | `unknown_resource_kind` |
| `operation` desconocida | `invalid_request` | — | `unknown_operation` |
| agente `deprecated` | `evaluated` | `denied` | `agent_deprecated` |

### 5. `policy_refs`

- La matriz de techo de acceso (sección 3) surge de **DEC-C11**.
- `requires_approval` está además sustentado por **DEC-C04**.
- Un `denied` (ej. `papu + project_drive + write`) surge del techo fijado por **DEC-C11**, no de DEC-C04 — no documentar DEC-C04 como si una aprobación pudiera superar un `denied`.

### 6. Dónde vive la política

Tabla constante/versionada **en código C#**. **No** agregar permisos a `papu.yml`, `chapu.yml` ni ningún YAML/config editable — un cambio del techo de autoridad es un cambio gobernado de código/Decision, no de configuración editable.

Policy global para V0: sin `project_id`, sin overrides por proyecto. Documentar esto como límite consciente en `## Resultado`, no como omisión.

## Fuera de alcance (no implementar en esta tarea)

Tocar Drive/GitHub reales; escribir estado real de proyectos; ejecutar efectos secundarios; `ApprovalEvidence`; persistir aprobaciones; gateway; Context Builder; sala/chat; llamadas a OpenAI/Anthropic; endpoint HTTP público nuevo; usar `skills`/`instructions`/`provider`/`model` como fuente de autorización; operaciones distintas de `read`/`write`.

## Criterio de éxito

- Tests explícitos para las 12 celdas de la matriz (sección 3), preferentemente uno por celda.
- Test: agente `deprecated` → `evaluated` / `denied` / `agent_deprecated`.
- Test: `agent_id` desconocido → `invalid_request` / `unknown_agent`.
- Test: `resource_kind` desconocido → `invalid_request` / `unknown_resource_kind`.
- Test: `operation` desconocida → `invalid_request` / `unknown_operation`.
- Test: `AgentRegistry` unavailable → `unavailable` / `agent_registry_unavailable`.
- Ninguna consulta a la política lanza una excepción no controlada.
- Test: cambiar `skills`/`instructions`/`provider`/`model` de un agente (en su YAML de TASK-0016) **no altera** el resultado de la política — confirma el desacoplamiento de capas.
- Test: Papu `write` sobre `project_drive` o `project_code_repo` es **siempre** `denied`, sin excepción.
- Test: Chapu `write` **nunca** es `allowed_without_approval` — siempre `requires_approval`.
- `dotnet build` sin errores; `dotnet test` sin regresiones sobre la suite existente (TASK-0001 a TASK-0016).

## Contexto / restricciones

- Depende de TASK-0016 (`AgentRegistry`) ya implementada — esta tarea consulta su estado, no lo reimplementa.
- Respetar DEC-C08: detalles técnicos menores (forma exacta de la tabla constante en C#, estructura interna de `PurePolicyMatrix`) quedan a criterio de Code, documentados en `## Resultado`.
- Si durante la implementación aparece una contradicción real con lo ya decidido, o un bloqueo técnico no previsto, señalarlo explícitamente en vez de resolverlo por criterio propio — no se espera que esta tarea reabra discusión de arquitectura salvo ese caso.

## Resultado

**Status: done.**

Ejecutado y commiteado directamente a `main` del repo de código (sin PR — commit directo): `2141c0a` — "TASK-0017: politica estatica de autorizacion de agentes (PurePolicyMatrix)". Ninguna contradicción nueva encontrada contra DEC-C04/DEC-C11/DEC-C16 durante la implementación; no hizo falta reabrir arquitectura ni tocar ninguna Decision desde este repo de código.

### Archivos nuevos/modificados

- `backend/ColaborIA.Api/Services/Agents/PolicyTypes.cs` (nuevo) — `AgentResourceKind`, `AgentOperation`, `PolicyEvaluationStatus`, `PolicyDecision`, `PolicyReasonCode`, `PolicyEvaluationResult`.
- `backend/ColaborIA.Api/Services/Agents/PurePolicyMatrix.cs` (nuevo) — capa A.
- `backend/ColaborIA.Api/Services/Agents/AgentAuthorizationService.cs` (nuevo) — capa B.
- `backend/ColaborIA.Api/Program.cs` (modificado) — registra `AgentAuthorizationService` como singleton en DI; **sin** endpoint HTTP nuevo.
- `backend/ColaborIA.Api.Tests/PurePolicyMatrixTests.cs`, `AgentAuthorizationServiceTests.cs` (nuevos).

### Cómo quedó implementado cada punto de la tarjeta

1. **`PolicyEvaluationResult`**: `Status` (`Evaluated`/`InvalidRequest`/`Unavailable`) y `Decision` (nullable, sólo poblado cuando `Status = Evaluated`) son campos separados — nunca un único enum mezclado. Incluye `ReasonCode` (enum machine-readable), `Detail` (texto humano) y `PolicyRefs` (`IReadOnlyList<string>`, plural como pedía la tarjeta).
2. **Vocabulario propio**: `AgentResourceKind` (`ProjectState`/`ProjectDrive`/`ProjectCodeRepo`) y `AgentOperation` (`Read`/`Write`) — enums de C#, no reutilizan `ProjectResource.Kind`/`ResourceCatalog`. La política nunca verifica si el recurso existe de verdad (eso queda para el futuro gateway).
3. **Matriz de 12 celdas**: implementada tal cual en `PurePolicyMatrix.Evaluate`, verificada 1:1 en `PurePolicyMatrixTests` (Theory con las 12 filas exactas).
4. **Dos capas separadas**: `PurePolicyMatrix` (estática, sin ninguna dependencia — ni siquiera conoce `AgentRegistry`) y `AgentAuthorizationService` (consulta `AgentRegistry`, valida, y sólo entonces delega en la matriz). `deprecated` se resuelve exclusivamente en la capa B, tal como pedía la tarjeta — la capa A jamás lo evalúa.
5. **`policy_refs`**: `RequiresApproval` cita `["DEC-C11", "DEC-C04"]`; `AllowedWithoutApproval` y `Denied` citan sólo `["DEC-C11"]` (`Denied` nunca cita DEC-C04 — verificado con test dedicado `Evaluate_DeniedDecision_CitesOnlyDecC11_NeverDecC04`). El caso `agent_deprecated` cita `["DEC-C16"]` (criterio de Code por DEC-C08: es el estado de identidad de Bloque C, no la matriz de accesos de DEC-C11).
6. **Dónde vive la política**: `Dictionary` constante dentro de `PurePolicyMatrix.cs`, en código C#. No se tocó ningún YAML de `config/agents/`. Sin `project_id` ni overrides por proyecto — límite documentado en el propio doc-comment de la clase, no un olvido.

### Decisión de diseño no explícita en la tarjeta (DEC-C08)

La tarjeta no cubría qué hacer si `AgentRegistry` resuelve un `agent_id` **activo y válido** que, aun así, no tiene ninguna fila en `PurePolicyMatrix` (ej. un tercer agente agregado a `config/agents/` en una tarea futura sin actualizar esta matriz). Se resolvió **fail closed por omisión**: la tabla devuelve `Denied` para cualquier combinación sin entrada explícita, nunca hereda `AllowedWithoutApproval`/`RequiresApproval` por default. Cubierto por el test `Evaluate_UnknownAgentId_FailsClosedToDenied`.

### Tests

`dotnet build`: 0 errores. `dotnet test`: **114/114** (88 preexistentes de TASK-0016 sin regresiones + 26 nuevos), incluyendo explícitamente todo lo pedido en `## Criterio de éxito`:

- las 12 celdas de la matriz, una por una (`PurePolicyMatrixTests`);
- `deprecated` → `Evaluated`/`Denied`/`AgentDeprecated`;
- `agent_id` desconocido → `InvalidRequest`/`UnknownAgent`;
- `resource_kind` desconocido → `InvalidRequest`/`UnknownResourceKind`;
- `operation` desconocida → `InvalidRequest`/`UnknownOperation`;
- `AgentRegistry` unavailable → `Unavailable`/`AgentRegistryUnavailable`, incluso con agentes válidos;
- ninguna consulta lanza excepción no controlada (incluye entradas `null`);
- cambiar `skills`/`instructions`/`provider`/`model` de un agente no altera el resultado (`Evaluate_ChangingSkillsInstructionsProviderOrModel_NeverChangesTheDecision`);
- Papu `write` sobre `project_drive`/`project_code_repo` siempre `Denied`;
- Chapu `write` nunca `AllowedWithoutApproval` — siempre `RequiresApproval`, en los tres `resource_kind`.

Verificado además en vivo (backend real, puerto separado del que ya tenías corriendo): `GET /health`, `GET /agents` y `GET /projects` siguen respondiendo `200` con el nuevo singleton `AgentAuthorizationService` registrado en DI, sin excepciones en el log de arranque — confirma que agregar esta infraestructura interna no afectó nada del Bloque B ni del resto de TASK-0016.

### Confirmación explícita de los boundaries pedidos

No se tocó Drive real, GitHub real, ni se escribió en la DB real de proyectos. No se implementó `ApprovalEvidence`, gateway, Context Builder, sala/chat, ni llamadas a OpenAI/Anthropic. No se agregó ningún endpoint HTTP público — `AgentAuthorizationService` es infraestructura interna, verificada exclusivamente por tests, disponible para que un consumidor futuro (Bloque D/E) la inyecte directo desde DI. No se agregaron permisos a `papu.yml`/`chapu.yml` ni a ningún YAML/config editable.

### Limitaciones reales restantes

- Política global únicamente (V0): sin `project_id` ni overrides por proyecto — documentado como límite consciente, no omisión (punto 9 de la tarjeta).
- `agent_deprecated` cita `DEC-C16` como `policy_ref` por criterio de Code (DEC-C08) — no estaba especificado en la tarjeta cuál Decision citar para ese caso puntual; puede ajustarse si Dani/Papu prefieren otra referencia.
- Ninguna otra limitación nueva detectada.
