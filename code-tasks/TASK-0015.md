---
id: TASK-0015
status: done
repo_destino: aleybru/proyecto-software-colaboria-codigo
creado: 2026-08-30
aprobado_por: Dani
depende_de: [TASK-0014]
decision_ref: DEC-C09, DEC-C16
reconstruccion_retrospectiva: true
id_historico_en_commits: "TASK-0014"
---

## Objetivo

**Nota de trazabilidad:** reconstrucción retrospectiva, escrita el 2026-08-30. Identificada en los commits del repo de código como **"TASK-0014"** — se conserva ese ID histórico como alias; el ID canónico en `code-tasks/` es **TASK-0015**.

Verificar escritura real, no solo lectura, en el repo de código recién creado: además de crear el repo y verificar acceso de lectura de la instalación (TASK-0014), inicializar un `README.md` real vía GitHub App y verificar la escritura leyendo el contenido de vuelta.

## Qué se hizo (reconstruido de la evidencia real)

- `GitHubAppService.ResolveInstallationAccessAsync` llama a `InitializeReadmeAsync` con el mismo installation access token: `GET` primero (idempotencia — si `README.md` ya existe, nunca se sobrescribe, ni el de ColaborIA ni uno editado a mano después); si no existe, `PUT /repos/{owner}/{repo}/contents/README.md` seguido de un segundo `GET` que compara el contenido releído contra lo escrito. Un `200`/`201` del `PUT` solo, sin la relectura, no se considera prueba de escritura.
- Contenido mínimo del README (`BuildReadmeContent`, método puro y testeado): solo el nombre del proyecto y una línea fija — sin asumir tecnología (sin `.gitignore`/`package.json`/`.csproj`/CI).
- Nuevo estado `github_repo_initialization_failed` cuando el repo y el acceso de lectura están OK pero la inicialización falla — conserva `external_ref` (nunca crea un segundo repo), se clasifica como error, y un reintento solo completa el README pendiente.

**Frontend:** nuevo caso en `project-detail` (`isGithubInitFailed`) con mensaje explicativo + botón "Reintentar" genérico — sin botón permanente de "Inicializar repositorio". `/projects/new` sin cambios.

## Resultado

**Status: done.**

Ejecutado y commiteado directamente a `main` (sin PR — commit directo).

**Evidencia verificable (citada del mensaje de commit original, con pruebas en vivo contra un repo real):**
- Commit: `0a16a6e` — "TASK-0014: inicializa README real via GitHub App e verifica escritura+lectura" (2026-08-30T15:18:10Z, autor `aleybru`).
- Verificado en vivo contra un proyecto real (`qa-readme-init`, repo real en `github.com/aleybru`): el `PUT` devolvió un commit real, el `GET` posterior devolvió el mismo contenido, y el commit quedó con autoría de `colaboriaapp[bot]` (identidad de instalación de la App, nunca un PAT).
- Confirmado contra GitHub real (no solo el estado persistido por ColaborIA): reintentar sobre el mismo recurso no agrega un segundo commit ni cambia `external_ref`.
- Editar el README a mano y forzar un reintento real (`resource.status` llevado a `pending_provisioning`) dejó el contenido editado a mano intacto, sin un segundo `PUT`.
- Revisados los logs del proceso: no aparecen tokens ni secrets en ningún momento.
- `dotnet build` (0 errores) + `dotnet test` (75/75) + `ng build` + `ng test` (7/7).

**Reconstrucción retrospectiva** — contenido tomado íntegramente del mensaje de commit original (`0a16a6e`). No se agregó ningún dato que no esté en ese mensaje.

---

**Cierre del Bloque B (DEC-C16):** con esta tarea, el Bloque B — Proyectos y aprovisionamiento queda cerrado. Ver `decisions/DEC-C16.md`, sección "Bloque B", marcada explícitamente como CERRADO/APROBADO por Dani el 2026-08-30.
