---
id: TASK-0023
status: pending
repo_destino: aleybru/proyecto-software-colaboria-codigo
creado: 2026-09-13
aprobado_por: Dani
depende_de: [TASK-0022]
decision_ref: DEC-C10, DEC-C16
---

## Objetivo

Segunda de tres tareas derivadas de la prueba de campo real. Agregar un modo de ronda nuevo, `both_sequential_with_reaction`, que permite un contraste breve entre Papu y Chapu sin abrir la puerta a una discusión abierta entre agentes. Cambia el contrato de routing — DEC-C10 sección 7 ya fue revisada (2026-09-13) antes de escribir esta tarjeta, no hace falta volver a discutir el principio, solo implementarlo.

## Contexto (no repetir la discusión, partir de acá)

Evidencia real: en la sesión de prueba, Papu nunca hizo referencia a lo que dijo Chapu en `both_sequential` — no por descuido de prompt, sino porque el orden fijo (Papu primero) hace que Papu no tenga esa información disponible todavía cuando le toca hablar. Feedback de Dani: se sentía como "dos alcahuetes hablándole solo a él", pidió contraste chico entre los dos, explícitamente **no** una discusión larga o recursiva ("no hay crédito ni bolsillo que soporte a dos IA conversando entre sí sin tiempo ni gloria").

Cierre de diseño completo en `handoffs/active/HANDOFF-2026-09-13-papu-to-chapu-task-0022-0024-cierre.md` y QC. Texto de DEC-C10 sección 7 ya actualizado con el modo nuevo — leerlo antes de implementar.

## Qué tiene que hacer Code (concreto)

### 1. Nuevo modo de ronda: `both_sequential_with_reaction`

Secuencia fija: Papu → Chapu → una única reacción corta de Papu al mensaje de Chapu. No es simétrico (Chapu no reacciona después) — coincide con el orden fijo ya vigente (Papu siempre primero).

### 2. Cero recursión como invariante de datos, no de código

**Requisito central de esta tarea, no negociable en la implementación:** el conjunto de estados/transiciones válidos de una ronda debe ser cerrado a nivel de modelo — algo equivalente a un enum fijo de modos (`papu_solo`, `chapu_solo`, `both_sequential`, `both_sequential_with_reaction`) donde no existe ninguna transición codificable que encadene una segunda reacción. No alcanza con que el código de orquestación "decida no continuar" — tiene que ser estructuralmente imposible representar una ronda con más de una reacción, de la misma forma que el floor lock (TASK-2019) no depende de que la lógica de aplicación "se acuerde" de chequear, sino de un constraint real.

### 3. Reutilizar la infraestructura ya existente, no duplicarla

- Floor lock (TASK-0019), atomicidad de claim (TASK-0020), `timeout_at`/recuperación de ronda (TASK-0020) se aplican igual al nuevo modo — no reimplementar, extender.
- Si el turno de reacción de Papu falla o vence timeout, aplica la misma regla de DEC-C10 sección 5 ya vigente: la ronda no continúa automáticamente como si la reacción hubiera llegado — requiere acción explícita, la ronda queda en estado terminal reflejando exactamente qué se completó y qué no.

### 4. Frontend

Agregar la opción `Ambos (Papu → Chapu → reacción de Papu)` (o etiqueta equivalente clara) al selector de destinatario de la sala, junto a las opciones existentes.

## Criterio de éxito

- Test: `both_sequential_with_reaction` ejecuta exactamente 3 turnos en el orden correcto (Papu, Chapu, Papu-reacción), cada uno viendo el contenido persistido de los anteriores.
- Test: no existe ningún camino de código o dato que permita una cuarta interacción de agente dentro de la misma ronda — intentarlo debe ser rechazado o simplemente no representable, no solo "no ocurrir en la práctica".
- Test: si el turno de reacción falla/vence timeout, la ronda queda en estado terminal correcto, sin fabricar ni esperar una reacción inexistente.
- Test: los modos existentes (`papu_solo`, `chapu_solo`, `both_sequential`) siguen funcionando exactamente igual, sin regresión.
- Verificación real (no solo tests): usar el modo nuevo desde la UI real y confirmar en la base de datos (vía Code, con acceso real) que la ronda y sus 3 turnos quedaron correctos — mismo nivel de verificación que se hizo con la sesión de prueba de TASK-0021.
- `dotnet build`/`dotnet test` y suite de frontend sin regresiones.

## Contexto / restricciones

- No implementar `both_blind` — sigue como capacidad futura, sin cambios en esta tarea.
- No implementar concisión/límite de output (eso es TASK-0022, debería estar implementada antes o en paralelo, según orden de ejecución que decida Dani) ni compactación de contexto (TASK-0024).
- Respetar DEC-C08: forma exacta de representar el enum de modos/estados en el esquema (tipo de columna, nombres exactos) queda a criterio de Code, documentada en `## Resultado` — el requisito no negociable es que sea un conjunto cerrado, no la forma exacta de implementarlo.
